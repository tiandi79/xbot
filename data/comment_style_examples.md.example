# 我的评论范例（few-shot）

> 复制本文件为 `data/comment_style_examples.md` 后编辑。
> 模型会模仿这里的「原帖 → 我的评论」配对，条数越多、越像你的真实发言，效果越好。
>
> 也可在 `.env` 里用 `COMMENT_STYLE_EXAMPLES_PATH` 指向任意路径。

## 格式规则

1. **每条范例用单独一行 `---` 分隔**（三个短横线）
2. 每条建议两行：
   - `原帖大意：` — 用一两句概括原帖在说什么（不必全文粘贴）
   - `我的评论：` — 写你会怎么回（60–110 字，与 style 文件立场一致）
3. 以 `#` 开头的段落会被忽略（可写说明文字）
4. 含「在此粘贴」「在此写你会」的占位块会被跳过
5. 脚本默认取**最后 4 条**有效范例发给模型

## 范例模板（复制 `---` 块追加即可）

---
原帖大意：某热帖观点明显违背常识，还装很有道理。
我的评论：这逻辑经不起推敲，把复杂问题简化成单一受害叙事了。根因不在这，别被带节奏。

---
原帖大意：作者说某段内容看不懂，发在 X 上求助大家解释。
我的评论：看不懂正常，主要在讲 A 和 B 的关系。你卡住的可能是中间那个概念——把 X 当成 Y 就顺了。

---
原帖大意：（在此粘贴原帖摘要）
我的评论：（在此写你会怎么回）
